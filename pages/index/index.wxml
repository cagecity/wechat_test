<view style="display:flex; justify-content:center">
    <view id="matrix">
        <button class="grid {{taped}}" wx:for="{{buttons}}" wx:for-item="itm" bind:tap="matrixTap">{{itm}}</button>
    </view>
</view>